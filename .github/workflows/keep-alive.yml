name: Keep Alive

on:
  schedule:
    - cron: "0 8 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend (hits Supabase DB)
        run: curl -sf --retry 3 --retry-delay 10 --max-time 60 https://miniklubben-api.onrender.com/api/colors > /dev/null
